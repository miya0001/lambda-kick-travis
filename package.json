{
  "name": "lambda-kicks-travis-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "gulp clean && gulp js && gulp mods && gulp zip",
    "deploy": "npm run build && gulp deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Takayuki Miyauchi",
  "license": "MIT",
  "dependencies": {
    "config": "^1.21.0",
    "superagent": "^2.0.0"
  },
  "devDependencies": {
    "del": "^2.2.0",
    "gulp": "^3.9.1",
    "gulp-install": "^0.6.0",
    "gulp-zip": "^3.2.0",
    "node-aws-lambda": "^0.1.8",
    "run-sequence": "^1.2.1"
  }
}
